//! Test submodules for `bench-compare` — MUST NOT require docker/orbstack/container.
//! Lightr-only workload runs use the SMALL fixture. Competitor path = detect+skip.

mod parsing_cells_table;
mod workload_runner;
