# JSON schemas (placeholder — R4-W4 fills the normative doc)

Normative agent-contract for every `--json` verb output and mcp tool input.
Each schema carries `x-lightr-schema-version`. `lightr schema` prints them.
